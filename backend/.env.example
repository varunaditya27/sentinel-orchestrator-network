# =============================================================================
# Sentinel Orchestrator Network (SON) - Environment Variables Template
# =============================================================================
#
# Copy this file to .env and fill in your actual API keys and configuration.
# NEVER commit the actual .env file to version control.
#
# Usage: cp .env.example .env
#
# =============================================================================

# =============================================================================
# GEMINI LLM CONFIGURATION (Optional - LLM features disabled if not set)
# =============================================================================
# Get your API key from: https://aistudio.google.com/apikey
# Remove the comments to enable LLM features
# GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_MODEL=gemini-2.5-flash
# LLM_ENABLED=true

# =============================================================================
# BLOCKFROST API CONFIGURATION (Optional - uses mock if not set)
# =============================================================================
# Get API keys from: https://blockfrost.io
# Mainnet API key for production Cardano network
# BLOCKFROST_MAINNET_API_KEY=mainnet_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Preview/Testnet API key
# BLOCKFROST_PREVIEW_API_KEY=preview_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Server host (default: 0.0.0.0)
# HOST=0.0.0.0

# Server port (default: 8000)
# PORT=8000

# Debug mode (default: false)
DEBUG=false

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# =============================================================================
# ORACLE AGENT CONFIGURATION
# =============================================================================

# Network: mainnet, preprod, or preview (default: preprod)
# ORACLE_NETWORK=preprod

# Fork detection threshold in blocks (default: 5)
# FORK_THRESHOLD=5

# =============================================================================
# NOTES FOR PRODUCTION
# =============================================================================
#
# 1. All environment variables are optional with graceful fallbacks
# 2. Without BLOCKFROST_MAINNET_API_KEY: system uses mock mainnet_tip
# 3. Without GEMINI_API_KEY: natural language explanations unavailable
# 4. Agents continue functioning with fallback implementations
# 5. For security: use environment secrets in production, not .env file
# 6. Set LOG_LEVEL=WARNING in production for reduced logging
#
